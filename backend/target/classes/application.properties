# ===================================================================
# MediCare Multispeciality Hospital Management System - Configuration
# ===================================================================

# Dynamic port binding for Render cloud deployment (${PORT}) with local fallback
server.port=${PORT:8081}

# Active profile configurable via environment with local zero-config fallback
# Use 'postgres' on Render, 'h2' for local zero-config, 'mssql' for local SQL Server
spring.profiles.active=${SPRING_PROFILES_ACTIVE:h2}

# -------------------------------------------------------------------
# JWT Security Configuration
# -------------------------------------------------------------------
app.jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# -------------------------------------------------------------------
# CORS Configuration
# -------------------------------------------------------------------
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,https://*.onrender.com}
