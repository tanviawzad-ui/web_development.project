# Production backend URL (used when deployed on Render or external hosting)
# Leave blank or omit for local development to use the Vite proxy (/api -> localhost:8081)
VITE_API_BASE_URL=https://medicare-backend.onrender.com/api
